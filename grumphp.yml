parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpmd:
            whitelist_patterns: []
            exclude: []
            ruleset: ['cleancode', 'codesize', 'naming']
            triggered_by: ['php']
        phpunit:
            config_file: ~
            testsuite: testsuite: test/Src/App
            group: []
            always_execute: false